 <!-- Weather + Stats -->
<div class="weather-card mb-4 rounded-8 overflow-hidden d-flex">
  <div class="w-100 d-flex justify-content-between align-items-center text-white mb-2">
    <span><i class="fa-solid fa-sun"></i> <%= @weather[:city] %>, <%= @weather[:temp] %>°C</span>
    <span class="badge bg-light text-dark"><%= @weather[:label] %></span>
  </div>
</div>
