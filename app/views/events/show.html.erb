<div class="container mt-4">
  <h1 class="mb-4">Événements proposés</h1>

  <div class="row">
    <% @events.each do |event| %>
      <div class="col-md-6 mb-4">
        <div class="card shadow-sm h-100">
          <div class="card-body">
            <h5 class="card-title"><%= event["title"] %></h5>
            <p class="card-text"><%= event["description"] %></p>
            <ul class="list-unstyled mb-0">
              <li>
                <strong>📅 Début :</strong>
                <%= event["starts_at"].to_time.strftime("%d/%m/%Y %H:%M") if event["starts_at"].present? %>
              </li>
              <li>
                <strong>⏰ Fin :</strong>
                <%= event["ends_at"].to_time.strftime("%d/%m/%Y %H:%M") if event["ends_at"].present? %>
              </li>
              <li><strong>📍 Lieu :</strong> <%= event["location"] %></li>
            </ul>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
