<header class="topbar d-flex justify-content-between align-items-center px-3 py-2">
  <!-- Burger menu -->
  <div class="topbar__burger">
    <button class="btn btn-link p-0" type="button" data-bs-toggle="offcanvas" data-bs-target="#sidebarMenu" aria-controls="sidebarMenu">
      <i class="fa-solid fa-bars fa-lg"></i>
    </button>
  </div>

  <!-- Titre centré -->
  <h1 class="topbar__title m-0 text-center flex-grow-1">
    <%= content_for?(:page_title) ? yield(:page_title) : "FIT BUDDY" %>
  </h1>

  <% if user_signed_in? %>
    <!-- Avatar + Dropdown -->
    <div class="dropdown">
      <a href="#" class="d-flex align-items-center text-decoration-none dropdown-toggle" id="userDropdown" data-bs-toggle="dropdown" aria-expanded="false">
        <% if current_user&.avatar&.attached? %>
          <%= image_tag current_user.avatar, class: "avatar me-2" %>
        <% else %>
          <div class="avatar-placeholder me-2"></div>
        <% end %>
        <span class="username"><%= current_user&.first_name %></span>
      </a>
      <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
        <li><%= link_to "Mon Profil", user_path(current_user), class: "dropdown-item" %></li>
        <li><%= link_to "Paramètres", edit_user_registration_path, class: "dropdown-item" %></li>
        <li><hr class="dropdown-divider"></li>
        <li><%= link_to "Déconnexion", destroy_user_session_path, method: :delete, class: "dropdown-item text-danger" %></li>
      </ul>
    </div>
  <% end %>
</header>
